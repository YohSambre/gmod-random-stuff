local function Yepeeeeee()
    local button = ents.Create( "grenade_helicopter" )
        button:SetPos( Vector( math.random( -4000, 4000 ), math.random( -4000, 4000 ), math.random( -4000, 4000 ) ) )
        button:Spawn()
    end
hook.Add( "Think", "ThisIsFine", Yepeeeeee )
